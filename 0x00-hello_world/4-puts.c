#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (sucess)
 */
 int main(void)
{
	char mots[] = "\"Programming is like building a multilingual puzzle";

	puts(mots);
	return (0);
}
