#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int j = 48;


	while (j < 58)
	{
		putchar(d);
		if (j != 57)
		{
			putchar(',');
			putchar(' ');
		}
		j++;
	}
	putchar('\n');

	return (0);
}
